export const buildTrackPermalink   = (handle: string, slug: string) => `/${handle}/${slug}`;
export const buildUserPermalink    = (handle: string)              => `/${handle}`;
export const buildPlaylistPermalink= (handle: string, slug: string) => `/${handle}/playlist/${slug}`;